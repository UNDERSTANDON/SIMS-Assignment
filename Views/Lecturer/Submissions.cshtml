@model List<SIMS_Assignment.Models.CourseRelatedModels.Submission>
@{
    ViewData["Title"] = "Assignment Submissions";
}

<div class="sims-page">
    <div class="sims-page-header">
        <h1 class="sims-page-title">Assignment Submissions</h1>
    </div>

    <div class="sims-card p-3">
        <table class="table">
            <thead>
                <tr>
                    <th>Student ID</th>
                    <th>Assignment Title</th>
                    <th>Submission Date</th>
                    <th>File</th>
                    <th>Grade</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var s in Model)
                {
                    <tr>
                        <td>@s.StudentId</td>
                        <td>@s.AssignmentTitle</td>
                        <td>@s.SubmissionDate.ToString("g")</td>
                        <td>
                            @if (string.IsNullOrEmpty(s.FilePath))
                            {
                                <span>-</span>
                            }
                            else
                            {
                                <a asp-action="DownloadSubmission" asp-route-submissionId="@s.Id">Download</a>
                            }
                        </td>
                        <td>@(s.Grade.HasValue ? s.Grade.ToString() : "-")</td>
                        <td>
                            <form asp-action="Grade" method="post" class="d-inline">
                                <input type="hidden" name="submissionId" value="@s.Id" />
                                <input type="number" name="grade" min="0" max="100" step="0.1" class="form-control d-inline w-25" placeholder="Grade" />
                                <input type="text" name="feedback" class="form-control d-inline w-50 ms-2" placeholder="Feedback" />
                                <button class="btn btn-sims-primary ms-2">Save Grade</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <a asp-action="Index" class="btn btn-sims-outline">Back</a>
    </div>
</div>
